import{FormEvent,useState}from'react';import{Loader2,LockKeyhole}from'lucide-react';import{supabase}from'../lib/supabase';import{useAuth}from'../context/AuthContext';import{Logo}from'../components/Logo';
export function SetPassword(){const{finishRecovery}=useAuth();const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[loading,setLoading]=useState(false);const[msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();if(password.length<8){setMsg('Use pelo menos 8 caracteres.');return}if(password!==confirm){setMsg('As senhas não coincidem.');return}setLoading(true);const{error}=await supabase.auth.updateUser({password});setLoading(false);if(error){setMsg(error.message);return}history.replaceState(null,'',location.pathname);finishRecovery()}return <main className="flex min-h-screen items-center justify-center px-5 py-10"><section className="w-full max-w-sm"><div className="mb-10 flex justify-center"><Logo/></div><div className="card p-6"><p className="mb-1 text-sm font-semibold text-cyan">Acesso protegido</p><h1 className="text-2xl font-extrabold">Defina sua nova senha</h1><p className="mt-2 text-sm text-muted">Use pelo menos 8 caracteres.</p><form onSubmit={submit} className="mt-6 space-y-4"><label><span className="label">Nova senha</span><div className="relative"><LockKeyhole className="absolute left-3 top-3.5 text-muted" size={19}/><input className="input pl-10" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password" required/></div></label><label><span className="label">Confirmar senha</span><input className="input" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} autoComplete="new-password" required/></label>{msg&&<p className="text-sm text-amber-200">{msg}</p>}<button disabled={loading} className="btn-primary w-full">{loading&&<Loader2 className="animate-spin" size={18}/>} Salvar senha</button></form></div></section></main>}
