import{useEffect,useMemo,useState}from'react';import{useNavigate}from'react-router-dom';import{getBatches,getInvestment,getSales,stats}from'../lib/data';import{money,periodRange}from'../lib/format';import type{InventoryBatch,Sale}from'../types';import{Top}from'./Products';
type Period='today'|'week'|'month'|'year';
export function Reports(){const nav=useNavigate();const[period,setPeriod]=useState<Period>('today');const[sales,setSales]=useState<Sale[]>([]);const[investment,setInvestment]=useState(0);const[batches,setBatches]=useState<InventoryBatch[]>([]);const[batchId,setBatchId]=useState('');useEffect(()=>{getBatches().then(setBatches)},[]);useEffect(()=>{if(batchId){getSales('1970-01-01T00:00:00.000Z','2999-01-01T00:00:00.000Z').then(x=>setSales(x.filter(s=>s.batch_id===batchId)));const b=batches.find(x=>x.id===batchId);setInvestment(b?.inventory_batch_items?.reduce((n,i)=>n+Number(i.quantity)*Number(i.unit_cost),0)||0)}else{const[a,b]=periodRange(period);Promise.all([getSales(a,b),getInvestment(a,b)]).then(([s,i])=>{setSales(s);setInvestment(i)})}},[period,batchId,batches]);const s=stats(sales);const products=useMemo(()=>{const m=new Map<string,number>();sales.filter(x=>x.status==='completed').flatMap(x=>x.sale_items||[]).forEach(i=>m.set(i.product_name,(m.get(i.product_name)||0)+i.quantity));return[...m.entries()].sort((a,b)=>b[1]-a[1]).slice(0,5)},[sales]);return <div><Top title="Relatórios" back={()=>nav(-1)}/><label className="mt-5 block"><span className="label">Visualizar por lote</span><select className="input" value={batchId} onChange={e=>setBatchId(e.target.value)}><option value="">Nenhum — usar período</option>{batches.map(b=><option key={b.id} value={b.id}>Lote {String(b.batch_number).padStart(4,'0')}</option>)}</select></label>{!batchId&&<div className="mt-4 grid grid-cols-4 gap-2">{([['today','Dia'],['week','Semana'],['month','Mês'],['year','Ano']]as const).map(([k,v])=><button key={k} onClick={()=>setPeriod(k)} className={`chip px-1 ${period===k?'chip-active':''}`}>{v}</button>)}</div>}<div className="mt-5 grid grid-cols-2 gap-3"><Metric t="Investido em estoque" v={money(investment)}/><Metric t="Receita bruta" v={money(s.revenue)} accent/><Metric t="Custo vendido" v={money(s.cost)}/><Metric t="Lucro bruto" v={money(s.grossProfit)} accent/><Metric t="Vendas" v={String(s.salesCount)}/><Metric t="Unidades vendidas" v={String(s.units)}/><Metric t="Ticket médio" v={money(s.averageTicket)}/><Metric t="Saldo receita − investimento" v={money(s.revenue-investment)}/></div><p className="mt-3 text-xs text-muted">Lucro bruto = receita das vendas − custo dos itens vendidos. O valor investido inclui as entradas feitas no período ou lote.</p><div className="card mt-5 p-5"><h2 className="font-bold">Por pagamento</h2><div className="mt-4 space-y-4"><Bar label="PIX" value={s.pix} max={s.revenue}/><Bar label="Dinheiro" value={s.cash} max={s.revenue}/><Bar label="Cartão" value={s.card} max={s.revenue}/></div></div><div className="card mt-5 p-5"><h2 className="font-bold">Produtos mais vendidos</h2><div className="mt-4 space-y-3">{products.map(([name,q],i)=><div key={name} className="flex items-center"><span className="mr-3 grid h-8 w-8 place-items-center rounded-lg bg-white/[.05] text-sm font-bold">{i+1}</span><span className="flex-1 font-semibold">{name}</span><b>{q} un.</b></div>)}{!products.length&&<p className="text-sm text-muted">Sem vendas nesta seleção.</p>}</div></div></div>}
function Metric({t,v,accent=false}:{t:string;v:string;accent?:boolean}){return <div className="metric"><p className="text-xs text-muted">{t}</p><p className={`mt-2 text-xl font-black ${accent?'text-cyan':''}`}>{v}</p></div>}function Bar({label,value,max}:{label:string;value:number;max:number}){return <div><div className="mb-2 flex justify-between text-sm"><span>{label}</span><b>{money(value)}</b></div><div className="h-2 rounded-full bg-white/[.05]"><div className="h-full rounded-full bg-electric" style={{width:`${max?Math.max(3,value/max*100):0}%`}}/></div></div>}
