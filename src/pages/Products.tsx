import{FormEvent,useEffect,useState}from'react';import{ChevronLeft,Plus,Pencil,Power}from'lucide-react';import{useNavigate}from'react-router-dom';import{getCategories,getProducts}from'../lib/data';import{supabase}from'../lib/supabase';import{money}from'../lib/format';import type{Category,Product}from'../types';import{Toast}from'../components/Toast';
const blank={name:'',description:'',unit_cost:'',sale_price:'',minimum_stock:'',category_id:''};
export function Products(){const nav=useNavigate();const[items,setItems]=useState<Product[]>([]);const[categories,setCategories]=useState<Category[]>([]);const[form,setForm]=useState(blank);const[editing,setEditing]=useState<Product|null>(null);const[open,setOpen]=useState(false);const[msg,setMsg]=useState('');const load=()=>Promise.all([getProducts(),getCategories()]).then(([p,c])=>{setItems(p);setCategories(c)});useEffect(()=>{void load()},[]);function edit(p:Product){setEditing(p);setForm({name:p.name,description:p.description||'',unit_cost:String(p.unit_cost),sale_price:String(p.sale_price),minimum_stock:p.minimum_stock==null?'':String(p.minimum_stock),category_id:p.category_id||''});setOpen(true)}async function save(e:FormEvent){e.preventDefault();const payload={name:form.name.trim(),description:form.description.trim()||null,unit_cost:Number(form.unit_cost),sale_price:Number(form.sale_price),minimum_stock:form.minimum_stock===''?null:Number(form.minimum_stock),category_id:form.category_id||categories[0]?.id||null};const{error}=editing?await supabase.from('products').update(payload).eq('id',editing.id):await supabase.from('products').insert(payload);if(!error){setMsg(editing?'Produto atualizado.':'Produto criado.');setOpen(false);setEditing(null);setForm(blank);void load()}else setMsg(error.message)}async function toggle(p:Product){await supabase.from('products').update({active:!p.active}).eq('id',p.id);void load()}return <div><Top title="Produtos" back={()=>nav(-1)} action={()=>{setEditing(null);setForm(blank);setOpen(true)}}/><p className="mt-2 text-sm text-muted">Cadastre e organize os itens por categoria.</p>{open&&<form onSubmit={save} className="card mt-5 space-y-4 p-5"><label><span className="label">Nome</span><input className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Ex.: Brigadeiro"/></label><label><span className="label">Categoria</span><select className="input" value={form.category_id} onChange={e=>setForm({...form,category_id:e.target.value})}><option value="">Sem categoria</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label><span className="label">Descrição (opcional)</span><input className="input" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><div className="grid grid-cols-2 gap-3"><label><span className="label">Custo unitário</span><input className="input" type="number" min="0" step="0.01" required value={form.unit_cost} onChange={e=>setForm({...form,unit_cost:e.target.value})}/></label><label><span className="label">Preço de venda</span><input className="input" type="number" min="0" step="0.01" required value={form.sale_price} onChange={e=>setForm({...form,sale_price:e.target.value})}/></label></div><label><span className="label">Estoque mínimo (opcional)</span><input className="input" type="number" min="0" step="1" value={form.minimum_stock} onChange={e=>setForm({...form,minimum_stock:e.target.value})}/></label><div className="flex gap-2"><button className="btn-primary flex-1">Salvar</button><button type="button" className="btn-secondary" onClick={()=>setOpen(false)}>Cancelar</button></div></form>}<div className="mt-5 space-y-2">{items.map(p=><div key={p.id} className={`card flex items-center gap-3 p-4 ${!p.active?'opacity-55':''}`}><div className="min-w-0 flex-1"><p className="font-bold">{p.name}</p><p className="text-xs text-cyan">{p.product_categories?.name||'Sem categoria'}</p><p className="mt-1 text-sm text-muted">{money(p.sale_price)} · Estoque {p.current_stock}</p></div><button onClick={()=>edit(p)} className="grid h-10 w-10 place-items-center rounded-xl bg-white/[.05]" aria-label="Editar"><Pencil size={18}/></button><button onClick={()=>toggle(p)} className="grid h-10 w-10 place-items-center rounded-xl bg-white/[.05]" aria-label="Ativar ou inativar"><Power size={18}/></button></div>)}{!items.length&&<div className="card p-6 text-center text-muted">Cadastre seu primeiro produto.</div>}</div>{msg&&<Toast message={msg}/>}</div>}
export function Top({title,back,action}:{title:string;back:()=>void;action?:()=>void}){return <div className="flex items-center gap-3"><button onClick={back} className="grid h-11 w-11 place-items-center rounded-xl bg-white/[.05]"><ChevronLeft/></button><h1 className="page-title flex-1">{title}</h1>{action&&<button onClick={action} className="grid h-11 w-11 place-items-center rounded-xl bg-electric"><Plus/></button>}</div>}
