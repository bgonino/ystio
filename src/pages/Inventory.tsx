import{FormEvent,useEffect,useState}from'react';import{ArrowDownToLine,AlertTriangle}from'lucide-react';import{Link,useNavigate}from'react-router-dom';import{getMovements,getProducts}from'../lib/data';import{supabase}from'../lib/supabase';import{dateTime}from'../lib/format';import type{InventoryMovement,Product}from'../types';import{Top}from'./Products';import{Toast}from'../components/Toast';
export function Inventory(){const nav=useNavigate();const[products,setProducts]=useState<Product[]>([]);const[moves,setMoves]=useState<InventoryMovement[]>([]);const[selected,setSelected]=useState('');const[qty,setQty]=useState('');const[cost,setCost]=useState('');const[msg,setMsg]=useState('');const load=()=>Promise.all([getProducts(true),getMovements()]).then(([p,m])=>{setProducts(p);setMoves(m)});useEffect(()=>{load()},[]);function choose(id:string){setSelected(id);const p=products.find(x=>x.id===id);if(p)setCost(String(p.unit_cost))}async function submit(e:FormEvent){e.preventDefault();const{error}=await supabase.rpc('register_inventory_entry',{p_product_id:selected,p_quantity:Number(qty),p_unit_cost:Number(cost),p_notes:null});if(error)setMsg(error.message);else{setMsg('Entrada registrada.');setQty('');load()}}return <div><Top title="Estoque" back={()=>nav(-1)}/><div className="mt-5 grid grid-cols-2 gap-3">{products.map(p=><div key={p.id} className="card p-4"><div className="flex items-start justify-between"><p className="font-bold">{p.name}</p>{p.minimum_stock!=null&&p.current_stock<=p.minimum_stock&&<AlertTriangle size={18} className="text-amber-300"/>}</div><p className="mt-3 text-3xl font-black">{p.current_stock}</p><p className="text-xs text-muted">unidades disponíveis</p></div>)}</div><form onSubmit={submit} className="card mt-5 space-y-4 p-5"><div className="flex items-center gap-2 font-bold"><ArrowDownToLine className="text-cyan" size={20}/>Nova entrada</div><label><span className="label">Produto</span><select className="input" required value={selected} onChange={e=>choose(e.target.value)}><option value="">Selecione</option>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><div className="grid grid-cols-2 gap-3"><label><span className="label">Quantidade</span><input className="input" required type="number" min="1" step="1" value={qty} onChange={e=>setQty(e.target.value)}/></label><label><span className="label">Custo unitário</span><input className="input" required type="number" min="0" step="0.01" value={cost} onChange={e=>setCost(e.target.value)}/></label></div><button className="btn-primary w-full">Confirmar entrada</button></form><div className="mb-3 mt-7 flex items-center justify-between"><h2 className="font-bold">Movimentações recentes</h2><Link className="text-sm text-electric" to="/products">Produtos</Link></div><div className="space-y-2">{moves.map(m=><div key={m.id} className="card flex items-center p-4"><div className="flex-1"><p className="font-semibold">{m.products?.name}</p><p className="text-xs text-muted">{label[m.movement_type]} · {dateTime(m.created_at)}</p></div><p className={`font-bold ${m.quantity>0?'text-cyan':'text-amber-200'}`}>{m.quantity>0?'+':''}{m.quantity}</p></div>)}</div>{msg&&<Toast message={msg}/>}</div>}
const label={entry:'Entrada',sale_out:'Venda',positive_adjustment:'Ajuste +',negative_adjustment:'Ajuste −',sale_cancellation:'Cancelamento'}
