import{useEffect,useMemo,useState}from'react';import{ChevronLeft,Minus,Plus,WalletCards,Banknote,CreditCard,CheckCircle2,Loader2}from'lucide-react';import{useNavigate}from'react-router-dom';import{getProducts}from'../lib/data';import{supabase}from'../lib/supabase';import{money}from'../lib/format';import type{PaymentMethod,Product}from'../types';
export function NewSale(){const nav=useNavigate();const[products,setProducts]=useState<Product[]>([]);const[cart,setCart]=useState<Record<string,number>>({});const[method,setMethod]=useState<PaymentMethod>('pix');const[received,setReceived]=useState('');const[confirm,setConfirm]=useState(false);const[loading,setLoading]=useState(false);const[done,setDone]=useState(false);const[error,setError]=useState('');useEffect(()=>{getProducts(true).then(setProducts)},[]);const total=useMemo(()=>products.reduce((n,p)=>n+(cart[p.id]||0)*Number(p.sale_price),0),[products,cart]);useEffect(()=>{setReceived(total?total.toFixed(2):'')},[total]);function change(p:Product,d:number){setCart(c=>({...c,[p.id]:Math.max(0,Math.min(p.current_stock,(c[p.id]||0)+d))}))}const chosen=products.filter(p=>cart[p.id]);async function finish(){if(Math.abs(Number(received)-total)>0.009&&!window.confirm(`O valor final é ${money(Number(received))}, diferente do total calculado de ${money(total)}. Confirmar mesmo assim?`))return;setLoading(true);setError('');const id=crypto.randomUUID();const{error}=await supabase.rpc('register_sale',{p_idempotency_key:id,p_payment_method:method,p_received_amount:Number(received),p_items:chosen.map(p=>({product_id:p.id,quantity:cart[p.id]}))});if(error){setError(error.message);setLoading(false);return}setDone(true);setLoading(false);setTimeout(()=>{setCart({});setConfirm(false);setDone(false);getProducts(true).then(setProducts)},1200)}if(done)return <div className="flex min-h-[65vh] flex-col items-center justify-center text-center"><CheckCircle2 size={72} className="text-cyan"/><h1 className="mt-5 text-3xl font-black">Venda registrada!</h1><p className="mt-2 text-muted">Estoque e dashboard atualizados.</p></div>;return <div><div className="flex items-center gap-3"><button onClick={()=>confirm?setConfirm(false):nav(-1)} className="grid h-11 w-11 place-items-center rounded-xl bg-white/[.05]"><ChevronLeft/></button><div className="flex-1"><p className="text-xs font-bold uppercase tracking-widest text-cyan">Fluxo rápido</p><h1 className="page-title">Nova venda</h1></div></div>{!confirm?<><div className="mt-5 space-y-3">{products.map(p=>{const q=cart[p.id]||0;return <div key={p.id} className={`card p-4 ${p.current_stock===0?'opacity-45':''}`}><div className="flex items-start justify-between"><div><p className="font-bold">{p.name}</p><p className="mt-1 text-sm text-muted">{money(p.sale_price)} · {p.current_stock} em estoque</p></div>{q>0&&<p className="font-bold text-cyan">{money(q*p.sale_price)}</p>}</div><div className="mt-4 flex items-center justify-end gap-4"><button disabled={!q} onClick={()=>change(p,-1)} className="grid h-11 w-11 place-items-center rounded-xl bg-white/[.06] disabled:opacity-30"><Minus/></button><span className="w-8 text-center text-xl font-black">{q}</span><button disabled={q>=p.current_stock} onClick={()=>change(p,1)} className="grid h-11 w-11 place-items-center rounded-xl bg-electric disabled:opacity-30"><Plus/></button></div></div>})}{!products.length&&<div className="card p-6 text-center text-muted">Cadastre produtos e faça uma entrada de estoque primeiro.</div>}</div><div className="sticky bottom-20 mt-5 rounded-2xl border border-electric/30 bg-panel2 p-4 shadow-soft"><div className="flex items-center justify-between"><span className="font-semibold text-muted">Total</span><span className="text-3xl font-black">{money(total)}</span></div><button disabled={!chosen.length} onClick={()=>setConfirm(true)} className="btn-primary mt-4 w-full">Revisar venda</button></div></>:<Review products={chosen} cart={cart} total={total} method={method} setMethod={setMethod} received={received} setReceived={setReceived} finish={finish} loading={loading} error={error}/>}</div>}
function Review({products,cart,total,method,setMethod,received,setReceived,finish,loading,error}:{products:Product[];cart:Record<string,number>;total:number;method:PaymentMethod;setMethod:(m:PaymentMethod)=>void;received:string;setReceived:(s:string)=>void;finish:()=>void;loading:boolean;error:string}){const diff=Number(received)-total;return <div><div className="card mt-5 p-5"><h2 className="text-lg font-bold">Resumo</h2><div className="mt-4 space-y-3">{products.map(p=><div key={p.id} className="flex justify-between text-sm"><span>{cart[p.id]}× {p.name}</span><b>{money(cart[p.id]*p.sale_price)}</b></div>)}</div><div className="mt-4 flex justify-between border-t border-white/10 pt-4"><span className="text-muted">Total calculado</span><b className="text-xl">{money(total)}</b></div></div><h2 className="mb-3 mt-6 font-bold">Forma de pagamento</h2><div className="grid grid-cols-3 gap-2"><button onClick={()=>setMethod('pix')} className={`btn-secondary h-16 px-2 ${method==='pix'?'border-electric bg-electric/15':''}`}><WalletCards/>PIX</button><button onClick={()=>setMethod('cash')} className={`btn-secondary h-16 px-2 ${method==='cash'?'border-electric bg-electric/15':''}`}><Banknote/>Dinheiro</button><button onClick={()=>setMethod('card')} className={`btn-secondary h-16 px-2 ${method==='card'?'border-electric bg-electric/15':''}`}><CreditCard/>Cartão</button></div><label className="mt-5 block"><span className="label">Valor final da venda</span><input className="input text-xl font-bold" type="number" min="0" step="0.01" value={received} onChange={e=>setReceived(e.target.value)}/><span className="mt-2 block text-xs text-muted">Ajuste aqui para aplicar desconto ou registrar outro valor negociado.</span></label>{method==='cash'&&diff>0&&<div className="mt-3 rounded-xl bg-cyan/10 p-4"><span className="text-sm text-muted">Troco</span><p className="text-2xl font-black text-cyan">{money(diff)}</p></div>}{Math.abs(diff)>0.009&&<p className="mt-3 rounded-xl bg-amber-400/10 p-3 text-sm text-amber-200">O valor final difere do total em {money(Math.abs(diff))}. Uma confirmação adicional será solicitada.</p>}{error&&<p className="mt-3 text-sm text-red-300">{error}</p>}<button disabled={loading||received===''} onClick={finish} className="btn-primary mt-6 h-14 w-full text-lg">{loading&&<Loader2 className="animate-spin"/>}CONFIRMAR VENDA</button></div>}
