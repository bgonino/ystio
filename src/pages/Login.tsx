import{FormEvent,useState}from'react';import{LockKeyhole,Mail,Loader2}from'lucide-react';import{Logo}from'../components/Logo';import{supabase,configured}from'../lib/supabase';
export function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[loading,setLoading]=useState(false);const[msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMsg('');const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setMsg(error.message==='Invalid login credentials'?'E-mail ou senha inválidos.':error.message);setLoading(false)}async function reset(){if(!email){setMsg('Informe seu e-mail primeiro.');return}const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:location.origin});setMsg(error?error.message:'Enviamos as instruções para seu e-mail.')}return <main className="flex min-h-screen items-center justify-center px-5 py-10"><section className="w-full max-w-sm"><div className="mb-10 flex justify-center"><Logo/></div><div className="card p-6"><p className="mb-1 text-sm font-semibold text-cyan">Bem-vindo de volta</p><h1 className="text-2xl font-extrabold">Entre para continuar</h1><p className="mt-2 text-sm text-muted">Suas vendas e seu estoque, sempre à mão.</p>{!configured&&<p className="mt-4 rounded-xl bg-amber-400/10 p-3 text-sm text-amber-200">Conecte as variáveis do Supabase para entrar.</p>}<form onSubmit={submit} className="mt-6 space-y-4"><label><span className="label">E-mail</span><div className="relative"><Mail className="absolute left-3 top-3.5 text-muted" size={19}/><input className="input pl-10" type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required placeholder="voce@email.com"/></div></label><label><span className="label">Senha</span><div className="relative"><LockKeyhole className="absolute left-3 top-3.5 text-muted" size={19}/><input className="input pl-10" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required placeholder="••••••••"/></div></label>{msg&&<p className="text-sm text-amber-200">{msg}</p>}<button disabled={loading||!configured} className="btn-primary w-full">{loading&&<Loader2 className="animate-spin" size={18}/>} Entrar</button><button type="button" onClick={reset} className="w-full py-2 text-sm font-semibold text-muted">Esqueci minha senha</button></form></div><p className="mt-6 text-center text-xs text-muted">Acesso pessoal e protegido. Não há cadastro público.</p></section></main>}
